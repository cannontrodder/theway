# theway

A fun, throwaway personal website. Small in scope — don't over-engineer it.

## Agent skills

### Issue tracker

Issues live as GitHub issues on `cannontrodder/theway`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` at the repo root (created lazily as decisions get made). See `docs/agents/domain.md`.
